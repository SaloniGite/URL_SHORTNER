<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shortened URL</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gradient-to-r from-indigo-500 to-blue-600 flex justify-center items-center min-h-screen">

    <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-md text-center">
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Your Shortened URL</h1>

    =
        <p class="text-lg text-gray-700 font-semibold">Shorten URL:</p>
        <a href="/shorten/<%= shortUrl %>" 
            class="text-blue-600 font-medium hover:text-blue-800 transition duration-200">
            <%= shortUrl %>
        </a>

        <div class="mt-6">
            <a href="/" 
               class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-6 rounded-lg transition duration-200 shadow-md">
                Shorten Another
            </a>
        </div>
    </div>

</body>
</html>
